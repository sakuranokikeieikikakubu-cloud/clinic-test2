# CLAUDE.md

このリポジトリは「さくらのき米山ファミリークリニック」の静的HTMLサイト(GitHub Pages公開用テストサイト)です。共通スタイルは `style.css` にまとめています。

## ヘッダー・フッター・ナビゲーションJSの共通化について

全ページ共通のヘッダー(`<header class="site-header">`〜`</header>`)、フッター+モバイルCTAバー(`<footer id="footer">`〜モバイルCTAバーの`</div>`)、ハンバーガーメニューの開閉JS(`<script>`内の最初の`(function(){...})();`)、`<head>`内の共通構造化データ(JSON-LD、`<!-- structured-data:start -->`〜`<!-- structured-data:end -->`)は、`partials/header.html`・`partials/footer.html`・`partials/nav-script.js`・`partials/structured-data.html` を正として管理しています。

各HTMLファイル自体には、これらのブロックの中身がそのまま(コピーではなく)書き出されています。**これらの共通部分を変更する場合は、`partials/` 配下のファイルを編集した後、必ず以下を実行してください。**

```
node build.js
```

このスクリプトが全HTMLファイル(トップレベルの `*.html` すべて)を走査し、`partials/` の内容を該当箇所に自動反映します。実行後は `git diff` で意図した差分だけになっているか確認してください。

`node build.js` を実行し忘れると、1ページだけ修正が反映され他のページに反映されない、という事故につながるため注意してください。

## 見た目に関わる変更を行うときのルール

CSS・レイアウト・デザインなど見た目に影響する変更(`style.css`の編集、HTMLの構造変更、フォント変更など)を行った場合は、**コミット・pushする前に必ず**以下を行うこと。

1. ブラウザ(またはヘッドレスブラウザでのスクリーンショット)で `index.html` を開いて確認する。変更が特定のページ固有の場合は、そのページも確認する。
2. 全ページ共通のヘッダー・フッター・`style.css`に影響する変更の場合は、`index.html`だけでなく他のページも最低1〜2ページ確認する。
3. デスクトップ幅だけでなく、ヘッダーのナビゲーションが折りたたまれるモバイル幅(1179px以下)でも見た目を確認する。(このリポジトリではクリニック名が長いため、ブレークポイントをclinic-test1の1099pxから1179pxに変更しています。)
4. 見出し・本文・ボタン・カードなどが重なったり、はみ出したり、崩れたりしていないかを目視でチェックする。
5. 確認が終わってから、変更内容をユーザーに報告する。コミット・pushはユーザーからの明示的な指示があってから行う。

崩れが見つかった場合は、コミット・push前に修正し、再度確認すること。
